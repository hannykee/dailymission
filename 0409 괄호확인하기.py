#0409 괄호확인하기 _ 정규식 문제

'''
is_pair함수는 문자열 s를 매개변수로 입력받습니다.
s에 괄호가 알맞게 짝지어져 있으면
True를 아니면 False를 리턴하는 함수를 완성하세요.
예를들어 s가 (hello)()면 True이고, )(이면 False입니다.
s가 빈 문자열("")인 경우는 없습니다.
'''

import re

def is_pair(s):
    s=re.compile("")
    while True:
        return True
    

s=str(input("문자열을 입력하세요"))
if str="":
    s=str(input("잘못된 입력입니다. 문자열을 입력하세요."))
